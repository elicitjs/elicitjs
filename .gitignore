# --- Dependencies ---
node_modules/
jspm_packages/
web_modules/

# --- Build & Distribution ---
# `npm pack` output — a local smoke-test artifact, never committed.
*.tgz
# Lib bundle (vite.lib.config.js) → dist/; docs site (vite.config.js) → site/.
dist/
site/
build/
out/
.parcel-cache/
.cache/
.vite/

# Docs live in sibling ../elicitjs-docs (own .gitignore)

# --- Environment Variables & Secrets ---
# Keep local environment configs, API keys, and credentials out of git
.env
.env.local
.env.*.local
*.pem

# --- Testing & Coverage ---
coverage/
.nyc_output/
.playwright/
.cypress/

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# --- IDEs and Editors ---
# Common developer files (can also be configured globally on your machine)
.vscode/
# (Optional) If you want to share VSCode tasks/debug configurations, uncomment the line below:
# !.vscode/launch.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- OS / System Files ---
.DS_Store
.DS_Store?
._*
ehthumbs.db
Thumbs.db
paper
